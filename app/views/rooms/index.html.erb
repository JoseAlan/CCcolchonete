<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <%= render @rooms %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path %>
